function mostrar() {

    var romper;
    var contador;

    for (romper = 0; romper != "romper"; romper++ ) {

        if (romper == 10 ) {

        romper = prompt ("Para detener el bucle, ingrese romper")

    }
}
}


}//FIN DE LA FUNCIÓN